<section>
  <div class="container">
    <table class="table mt-5" id="myTable">
      <thead>
        <tr>
          <th scope="col">No</th>
          <th scope="col">Category Name</th>
          <th scope="col">Offer %</th>

          <th>Remove Offer</th>

        </tr>
      </thead>
      <tbody>
        {{#each categories}}
        <tr>
          <th scope="row">{{inc @index}}</th>
          <td>{{this.Category}}</td>
          {{#if this.categoryoffer}}
          <td> {{this.categoryoffer}}</td>
          {{else}}
           <td> nil</td>
          {{/if}}

           {{#if this.categoryoffer}}
          <td><a href="/admin/removecategoryoffer/{{this._id}}" class="btn btn-danger" >Remove </a></td>
             {{else}}
          {{!-- <td><a href="#" class="btn btn-danger" style="pointer-events: none;">Remove </a></td> --}}
          <td>nil</td>
          {{/if}}

        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</section>

</div>
</div>
</div>

<script>

</script>