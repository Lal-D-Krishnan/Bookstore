
        <div class="login-register-area pt-50 pb-40">
            <div class="container">
                <div class="row">
                    <div class="col-lg-7 col-md-12 ms-auto me-auto">
                        <div class="login-register-wrapper">
                            <div class="login-register-tab-list nav">
                                <a href="#">
                                    <h4> Edit Address </h4>
                                </a>
                            </div>
                            <div class="tab-content">
                                <div id="lg2" class="tab-pane active">
                                    <div class="login-form-container">
                                        <div class="login-register-form">
                                            <form action="/editaddress/{{indAddr._id}}" method="post">
                                                {{!-- <input type="text" name="userId" id="" value="{{registeredUser._id}}" hidden> --}}

                                                <input type="text" name="address" placeholder="Address" value="{{indAddr.address}}">
                                                <input type="text" name="mobile" placeholder="Mobile" value="{{indAddr.mobile}}">
                                                <input type="text"  name="pincode" placeholder="Pincode" value="{{indAddr.pincode}}">
                                                <div class="button-box">
                                                    <button type="submit">Apply Changes</button>
                                                </div>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>    
                        </div>
                    </div>
                </div>
            </div>
        </div>